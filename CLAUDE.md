# CLAUDE.md

웹 대시보드 세 개를 담는 저장소. **세 프로젝트는 서로 독립이다.**

| 프로젝트 | 산출물 | 요구사항 | 태스크 | 검증 스크립트 | 저장 |
|---|---|---|---|---|---|
| 관심종목 모닝 대시보드 | `관심종목_대시보드.html` | @PRD.md | @TASKS.md | `_test_inject.js` | localStorage `morning-dashboard-v1` |
| 오늘의 건강 대시보드 | `헬스케어_대시보드.html` | @PRD_헬스케어.md | @TASKS_헬스케어.md | `_health_test_inject.js` | Supabase (로그인) / localStorage `health-dashboard-local-v1` (로컬 모드) |
| 인사노무 법령 브리핑 | `직무AI_대시보드.html` + `api/` | @PRD_직무AI.md | @TASKS_직무AI.md | `_job_test_inject.js` | Supabase `qplxvfwuobvnobqpivbm` (`labor_*` 테이블) |

**앞의 두 개는 1인용이고, 직무AI는 팀 공유용이다.** 이 차이가 저장·권한 설계를 가른다.

**헬스케어와 직무AI는 배포 대상이다.** Vercel → `vercel.json` 이 `/` 를 헬스케어로,
`/labor` 를 직무AI로 보낸다.
배포에 영향을 주는 변경(파일명, 라우팅, 키)은 `vercel.json` 과 함께 확인한다.

작업을 시작하기 전에 **어느 프로젝트인지 먼저 확정하고**, 해당 PRD를 확인한다.
요청이 PRD와 충돌하면 임의로 판단하지 말고 먼저 알린다.
한 프로젝트를 수정하면서 다른 프로젝트 파일을 건드리지 않는다.

> `직무AI_대시보드.html` 은 2026-08-13 기준 **헬스케어 대시보드의 복사본**이다 (내용 미착수).
> 태스크 1에서 백지부터 다시 쓴다.

---

## 기술 규칙

아래 규칙은 세 프로젝트에 공통 적용된다. **단일 파일 규칙만 직무AI가 예외다** (바로 아래 참조).

- **순수 HTML / CSS / JS만 사용한다.** 프레임워크, 빌드 도구, 외부 라이브러리, CDN 링크를 도입하지 않는다.
  - 직무AI의 서버 함수(`api/`)도 순수 JS다. 빌드 도구·패키지 의존을 넣지 않는다.
- **각 프로젝트의 산출물은 위 표의 HTML 단일 파일로 유지한다.** 파일을 쪼개거나 늘리지 않는다.
  - 마크업, `<style>`, `<script>` 를 한 파일 안에 둔다.
  - 파일 하나만 챙기면 되도록 하는 것이 목적이다 — 더블클릭 실행, 복사, 백업, 첨부가 모두 한 번에 끝나야 한다.
  - CSS 색상은 `:root` 의 CSS 변수로 선언하고, 색상값을 규칙 안에서 직접 반복하지 않는다.
  - JS는 단일 IIFE 안에 두고 전역 변수를 만들지 않는다.
  - 예외: 검증 스크립트(`_test_inject.js`, `_health_test_inject.js`, `_job_test_inject.js`)는 산출물이 아니므로 별도 파일로 둔다. 원본 HTML에는 절대 포함하지 않는다.
  - **예외: 직무AI는 서버 함수를 갖는다.** 자동 수집(법제처 API)과 팀 공유가 서버를 요구하기 때문이다.
    화면은 여전히 `직무AI_대시보드.html` 단일 파일로 유지하고, 서버 코드만 `api/` 에 둔다.
    - 법제처 API 는 `http://` 엔드포인트 + CORS 차단 + `OC` 키 노출 문제로 **브라우저에서 직접 호출할 수 없다.**
    - 비밀값(`OC` 키, service 키)은 Vercel 환경변수에만 둔다. 클라이언트로 내려보내지 않는다.
    - 관심종목·헬스케어의 단일 파일 규칙은 **그대로 유지된다.** 이 예외를 다른 프로젝트로 넓히지 않는다.
- **저장 위치는 프로젝트마다 다르다.** 위 표의 "저장" 열을 따른다.
  - 관심종목: localStorage 전용. 저장 키 하나로 유지하고, 스키마가 바뀌면 키 버전을 올리고 마이그레이션을 넣는다.
  - 헬스케어: Supabase(계정별) + 로그인 없이 쓸 때는 localStorage. 배포 대상이라 사용자 구분이 필요하다.
    - Supabase 접근은 **라이브러리 없이 `fetch`로 PostgREST/Auth REST API를 직접 호출한다.** CDN 금지 규칙은 그대로다.
    - 테이블을 추가·변경하면 **RLS 정책을 반드시 함께 넣고**, `get_advisors`로 확인한다.
    - 코드에 넣어도 되는 키는 **publishable 키뿐이다.** service_role 키는 절대 넣지 않는다.
  - 직무AI: Supabase 전용 (`qplxvfwuobvnobqpivbm`, 서울). 팀 공유라 localStorage 로는 성립하지 않는다.
    - 테이블은 `labor_` 접두어를 쓴다. 같은 프로젝트에 헬스케어 태스크 12(되돌림)의 잔여
      테이블 7개(`profiles` · `daily_logs` · `meals` · `workouts` · `memos` ·
      `checklist_items` · `checklist_marks`)가 남아 있다 — **건드리지 않는다.**
    - **읽기에도 로그인이 필요하다.** RLS 읽기 정책을 `authenticated` 로 제한하고 `anon` 에 열지 않는다.
    - 화면에 **가입 기능을 만들지 않는다.** 계정은 관리자가 Supabase 대시보드에서 발급한다
      (`mailer_autoconfirm: false` 라 가입 API 는 확인 메일에서 막힌다 — 헬스케어 태스크 12가 여기서 좌초했다).
- 사용자 입력은 `textContent` 로 삽입한다. `innerHTML` 문자열 조립 금지.
- 라이트 / 다크 모드를 함께 유지한다. 한쪽만 확인하고 끝내지 않는다.

---

## 작업 규칙

- **한 번에 한 태스크만 진행한다.** 여러 태스크를 묶어 처리하지 않는다.
- 시작 전 해당 프로젝트의 태스크 문서에서 대상 태스크와 완료 기준을 확인한다.
- **변경 후에는 반드시 브라우저에서 동작을 확인한다.** 코드만 수정하고 완료로 보고하지 않는다.
  - 확인 기준은 해당 PRD 7항을 따른다.
  - 검증 스크립트와 실행 방법은 해당 태스크 문서 하단에 있다.
- 기능을 추가하거나 수정하면 해당 프로젝트의 검증 스크립트에 검증 항목을 함께 추가한다.
- 태스크가 끝나면 해당 태스크 문서의 상태와 검증 결과를 갱신한다.
- **요청 범위를 넘는 리팩터링이나 기능 추가는 하지 않는다.** 필요해 보이면 먼저 제안하고 승인을 받는다.
- 검증하지 못한 항목은 "완료"로 적지 않는다. 미검증으로 표시하고 사유를 남긴다.

---

## 설계 원칙

- **마찰을 줄이는 것이 기능 추가보다 우선한다.** 관심종목은 아침 5분, 헬스케어는 하루 여러 번 짧게,
  직무AI는 아침 5분 훑어보기다. (각 PRD 2항)
  - 관심종목·헬스케어는 **입력** 마찰을 줄인다 → 커서 자동 포커스, Enter 완결.
  - 직무AI는 **읽기** 마찰을 줄인다 → 대부분의 사용자는 아무것도 입력하지 않는다.
    찾아다니지 않게 하는 것이 목적이므로 훑어보기 속도와 정보 밀도가 우선이다.
- 마우스보다 키보드. 추가·저장 동작은 Enter 또는 단축키로 완결되게 한다.
- 입력이 주된 화면은 열면 커서가 첫 입력창에 자동으로 놓인다.
- 카테고리 구분 방식은 프로젝트마다 다르다. **해당 PRD 4항을 따른다.**
  - 관심종목: 카테고리 색으로 구분
  - 헬스케어: Airbnb DLS 단일 액센트 원칙에 따라 색 대신 일러스트 플레이트로 구분
  - 직무AI: Clay 다색 시스템에 따라 4카드를 채도 높은 색으로 구분 (teal · ochre · lavender · peach).
    `brand-pink` 는 흰 글씨 대비가 3.1:1 로 미달해 본문 카드에 쓰지 않는다
- **직무AI는 법률 정보를 다룬다.** 원문 링크를 항상 함께 두고, AI 요약에는 라벨을 붙인다.
  "법률 자문이 아님"을 화면에 상시 노출한다. 잘못된 요약이 실무 판단에 쓰이면 직접 피해가 간다.

---

## 디자인 지시

`DESIGN-<브랜드>.md` 파일이 저장소에 있다. 브랜드 디자인 적용을 요청받으면
**반드시 해당 문서를 먼저 읽고** 그 안의 토큰값(색·타이포·radius·elevation)을 그대로 쓴다.
브랜드를 기억으로 추정해 팔레트를 지어내지 않는다.

문서의 원칙이 PRD와 충돌하면(예: 단일 액센트 vs 카테고리 7색) 임의로 절충하지 말고
어느 쪽을 따를지 먼저 알린 뒤 PRD를 갱신한다.
