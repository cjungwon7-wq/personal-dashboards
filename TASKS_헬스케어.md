# 태스크 목록 — 오늘의 건강 대시보드

관련 문서: [PRD_헬스케어.md](./PRD_헬스케어.md) · 산출물: `헬스케어_대시보드.html`
최종 갱신: 2026-08-12

**완료 기준은 PRD_헬스케어 7항을 따른다** — 브라우저에서 해당 기능이 실제로 동작하면 완료.

---

## 진행 현황

| # | 태스크 | 상태 | 검증 |
|---|---|---|---|
| 1 | 화면 뼈대 (상단·좌·우·하단) | ✅ 완료 | 라이트/다크 렌더링 확인 |
| 2 | 식단 기록 | ✅ 완료 | 3/3 통과 |
| 3 | 수면 기록 | ✅ 완료 | 2/2 통과 |
| 4 | 운동 기록 | ✅ 완료 | 4/4 통과 |
| 5 | 물 섭취 기록 | ✅ 완료 | 3/3 통과 |
| 6 | 메모 · 체크리스트 | ✅ 완료 | 6/6 통과 |
| 7 | 오늘의 점수 (10점 만점) | ✅ 완료 | 3/3 통과 |
| 8 | 디자인 · 카테고리 색 구분 (초판) | ✅ 완료 | 라이트/다크 스크린샷 확인 |
| 9 | Starbucks 디자인 적용 | ⛔ 폐기 | 태스크 10으로 대체 |
| 10 | Airbnb 디자인 적용 · 이모지 · 일러스트 | ✅ 완료 | 22/22 재통과 + 라이트/다크 스크린샷 |
| 11 | 레이아웃 재구성 · 운동 종류 · 점수 표정 | ✅ 완료 | 32/32 + 마이그레이션 6/6 |
| 12 | 배포용 Supabase 저장 전환 (로그인 · 날짜별 기록) | ⛔ 되돌림 | 태스크 14로 제거 |
| 13 | Vercel 404 수정 (`vercel.json` 라우팅) | ⚠️ 배포 확인 필요 | 로컬 검증 불가 |

**전체 자동 검증: 66 / 66 통과** (Edge headless, 2026-08-12)
**v2 → v3 마이그레이션 검증: 10 / 10 통과**

---

## 태스크 12 — 배포용 Supabase 저장 전환 ✅

**요청**: 배포를 전제로 한 테이블을 만들고 localStorage 저장을 Supabase로 교체.

**결정 사항** (사용자 확인)

| 항목 | 선택 |
|---|---|
| 사용자 구분 | Supabase Auth 이메일 로그인 + 전 테이블 `user_id` RLS |
| 지난 날 기록 | `log_date` 기준으로 쌓아 보존 |

**스키마** — PRD_헬스케어 6항 참조. 7개 테이블, 전부 RLS.

**설계에서 챙긴 것**

1. **완료 표시를 `checklist_marks(item_id, log_date)` 로 분리했다.**
   그 날짜 행이 없으면 체크가 저절로 풀리므로 자정 초기화 로직 자체가 없어졌다.
2. **쓰기는 낙관적 + 직렬 큐.** 화면을 먼저 갱신하고 요청은 한 줄로 세워 보낸다.
   기록 직후 삭제처럼 순서가 뒤집히면 안 되는 조합이 있어서 병렬로 보내지 않는다.
3. **수면·물은 400ms 디바운스.** `input` 이벤트가 타이핑마다 터지므로 모아서 한 번 보낸다.
   둘 다 `daily_logs` 한 행이라 upsert 하나로 끝난다.
4. **401이면 토큰을 강제 갱신하고 1회 재시도한다.** 로컬 시계가 어긋나도 화면 오류로 만들지 않는다.
5. 행 `id` 는 클라이언트에서 uuid로 만든다. 서버 응답을 기다리지 않고 화면을 먼저 그리기 위해서다.

| 검증 항목 | 결과 |
|---|---|
| 로그인 전 앱 가려짐 · 잘못된 비밀번호 거부 | PASS |
| 로그인 성공 시 앱 진입 · 토큰 저장 | PASS |
| 가입 트리거가 기본 체크 4종 생성 | PASS |
| 식단 · 수면 · 운동 · 수분 · 메모 · 체크 (기존 32항목 상당) | PASS |
| 오늘의 점수 8.8 · 표정 · 상단 동기화 | PASS |
| Supabase 각 테이블 저장 내용 대조 | PASS |
| 등록 직후 삭제분이 서버에 남지 않음 (요청 순서 보장) | PASS |
| 기록이 날짜별로 분리됨 (어제 조회 시 0건) | PASS |

**작업 중 잡은 것**

- 첫 실행에서 6개 병렬 GET 중 `profiles` 하나만 401이 났다. 하나라도 실패하면 전체 로드가
  실패해 화면이 비어 보였다 → 401 재시도를 넣었다.
- 검증 스크립트가 디바운스보다 먼저 `daily_logs` 를 읽어 실패로 잡혔다. 코드가 아니라
  테스트의 경합이었다 (서버 행은 정상). 대기 조건에 `daily_logs` 를 추가했다.
- 검증 스크립트에 테스트 계정 비밀번호가 박혀 있었다. 이 파일은 저장소에 있고 배포되면
  공개되므로, 환경변수로 주입받도록 바꾸고 `.vercelignore` 로 배포에서 제외했다.

---

## 태스크 13 — Vercel 404 수정 ⚠️

**증상**: 배포 후 루트 접속 시 `404: NOT_FOUND`.

**원인**: 저장소 루트에 `index.html` 이 없다. Vercel은 URL 경로를 파일로 매핑하므로
`/` 는 `index.html` 을 찾고, 없으면 404다. HTML 파일명이 한글이라 그 경로로만 열렸다.

> 처음 의심한 두 가지는 원인이 아니었다.
> HTML 파일 개수는 404와 무관하고, `main` 은 `origin/main` 과 동기화돼 있었다.
> 다만 Supabase 전환분이 커밋되지 않아 배포본이 옛 버전이었던 것은 별개의 실제 문제였다.

**조치**: `vercel.json` 추가.

| 경로 | 대상 |
|---|---|
| `/` | `헬스케어_대시보드.html` |
| `/health` | `헬스케어_대시보드.html` |
| `/stocks` | `관심종목_대시보드.html` |

보안 헤더(`nosniff`, `Referrer-Policy`, `X-Frame-Options`)도 함께 넣었다.

**미검증**: 로컬에서 Vercel 라우팅을 확인할 방법이 없다. 배포 후 `/` 접속으로 확인해야 한다.
한글 파일명 rewrite가 문제를 일으키면 파일명을 ASCII로 바꾸는 것이 확실한 해법이다.

---

## 태스크 1 — 화면 뼈대 ✅

상단 배너(인사·날짜·테마 토글), 좌측 핵심기능 4카드, 우측 메모·체크리스트,
하단 전체 폭 점수 카드의 2단 그리드. 860px 이하에서 1단 전환.

**완료 기준**: PRD 4항 화면 구성대로 렌더링될 것 → 충족

---

## 태스크 2 — 식단 기록 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 식단 기록 | 아침·점심·저녁·간식 4건 입력 → 목록 4건 | PASS |
| 끼니 구분 저장 | 첫 행 배지가 `아침` | PASS |
| 식단 삭제 | 간식 행 `×` → 3건 | PASS |

---

## 태스크 3 — 수면 기록 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 수면 시간 자동 계산 | 23:00 → 06:00 입력 시 `7시간 0분` (자정 넘김) | PASS |
| 목표 달성 배지 | 7시간 이상일 때 `목표 달성` 표시 | PASS |

---

## 태스크 4 — 운동 기록 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 운동 기록 | 러닝 30분 · 요가 15분 → 2건 | PASS |
| 시간 합계 | `45분` 표시 | PASS |
| 0분 입력 차단 | 0분 제출 → 목록 변화 없음 | PASS |
| 삭제 후 합계 갱신 | 요가 삭제 → 1건, `30분` | PASS |

---

## 태스크 5 — 물 섭취 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 잔 수 증가 | `+` 5회 → `5 / 8잔` | PASS |
| 잔 아이콘 반영 | 채워진 잔 5개 | PASS |
| 0잔 미만 방지 | `−` 8회 → `0 / 8잔` | PASS |

---

## 태스크 6 — 메모 · 체크리스트 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 메모 저장 | 2건 저장 → 2건, 배지 갱신 | PASS |
| 메모 삭제 | `×` → 1건 | PASS |
| 기본 체크 항목 로드 | 최초 실행 시 4개 생성 | PASS |
| 체크 토글 · 진행률 | 2개 체크 → `2 / 4` | PASS |
| 항목 추가 | 입력 → 5개 | PASS |
| 항목 삭제 | `×` → 4개 | PASS |

---

## 태스크 7 — 오늘의 점수 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 총점 계산 | 수면 7h(2.5) + 운동 30분(2.5) + 식단 3끼(2.5) + 물 4/8잔(1.25) = `8.8` | PASS |
| 항목별 막대 4행 | 수면 · 운동 · 식단 · 물 | PASS |
| 부분 점수 환산 | 물 4/8잔 → `1.3` | PASS |

---

## 태스크 8 — 디자인 (초판) ✅

카테고리별 색을 카드 좌측 바·제목 점·배지·버튼·포커스 링에 일관 적용.
그린 그라데이션 배너, 14px 라운드, 카드 그림자.

다크 모드에서 밝은 카테고리 색 위 흰 글씨의 대비가 부족해 `--on-cat` 변수를 도입하고
`.addbtn` · `.segbtn[aria-pressed=true]` 글자색을 테마별로 뒤집었다.

> 태스크 9에서 Starbucks 디자인으로 교체됨. 색상값은 PRD 4항 표가 최신이다.

---

## 태스크 9 — Starbucks 디자인 적용 ⛔ 폐기

**요청**: `DESIGN-starbucks` — 스타벅스 브랜드 디자인 언어로 재적용.

CSS 변수값과 형태만 교체했다. **마크업·JS·ID·클래스는 변경하지 않았으므로 기능 회귀 없음.**

**색**
- 라이트: 웜 크림 배경(`#F5F2EC`) + 화이트 카드, 텍스트 `#27251F`
- 다크: 중성 회색 대신 하우스 그린 표면(`#0E1C18` / `#1A2C26`)
- 배너: 하우스 그린 → 스타벅스 그린 그라데이션 + 우상단 애센트 그린 광원
- 카테고리: 에스프레소 / 하우스 그린 / 사이렌 레드 / 아이스 틸 / 슬레이트 스톤 / 스타벅스 그린 / 리워드 골드

**형태**
- 추가·저장 버튼을 완전 라운드 필(`999px`)로 변경
- 물 잔·스테퍼를 원형(`50%`)으로 변경
- 카드 라운드 14px → 16px, 배너 라운드 18px → 22px
- h1 23px → 26px, 자간 `-0.03em`

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 기능 회귀 없음 | 리스타일 후 전체 검증 재실행 | 22 / 22 PASS |
| 라이트 렌더링 | 시드 데이터 스크린샷 확인 | PASS |
| 다크 렌더링 | 시드 데이터 스크린샷 확인 | PASS |

**작업 중 수정한 것**: 첫 적용에서 메모(스톤 `#5A5751`)와 식단(에스프레소 `#5B4034`)의
명도·색상이 가까워 카드 구분이 약했다. 메모를 쿨한 슬레이트(`#55696B` / `#A3BBBD`)로 바꿔 분리했다.

> **폐기 사유**: 저장소에 `DESIGN-starbucks.md` 스펙이 있었는데 읽지 않고 브랜드를 임의 해석했다.
> 사용자 피드백으로 태스크 10(Airbnb)으로 전면 교체. 앞으로 `DESIGN-*.md` 지시가 오면
> **해당 문서를 먼저 읽고** 토큰값을 그대로 쓴다.

---

## 태스크 10 — Airbnb 디자인 적용 ✅

**요청**: `DESIGN-airbnb.md` 기준으로 재디자인 + 인사말 이모지 + 텍스트·이미지 병행(부드럽고 young한 톤).

**적용한 DLS 규칙**
- 흰 캔버스 + ink(`#222`) + **Rausch(`#FF385C`) 단일 액센트**. Rausch는 브랜드 마크·기본 CTA·전량 완료에만.
- 타이포: 인사말 28/700, 카드 제목 20/600(display-sm), 본문 16/400, 메타 14/400, 배지 11/600.
  Cereal VF 대체는 문서 권고대로 Inter → 시스템 스택.
- 형태: 버튼 8px, 카드 14px, 필·잔·스테퍼 원형. 하드 코너 없음.
- 엘리베이션은 문서대로 **단 한 단계**(`--lift`), 카드 hover 전용.
- 입력 포커스는 글로우 링 없이 ink 테두리만 두껍게 (`text-input` 토큰).
- 오늘의 점수 = `rating-display` 64px/700 + 월계수 장식.

**이모지 인사** — 시간대별 6단계 (🌙 새벽 / ☀️ 아침 / 🥗 점심 / 🌤️ 오후 / 🌆 저녁 / 🌙 밤).

**이미지** — 히어로 1개 + 식단·수면·운동·물 플레이트 4개, 총 5개 인라인 SVG 일러스트.
파스텔 그라데이션 + 둥근 형태로 young한 톤. 외부 리소스 금지 규칙 때문에 실사진 대신 사용.

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 기능 회귀 없음 | 재디자인 후 전체 검증 재실행 | 22 / 22 PASS |
| 라이트 렌더링 | 시드 데이터 전체 높이 스크린샷 | PASS |
| 다크 렌더링 | 시드 데이터 전체 높이 스크린샷 | PASS |

**작업 중 수정한 것**
1. 러너 일러스트의 팔다리가 어색해 사이드 프로필 실루엣으로 다시 그렸다.
2. 좌측 4카드 / 우측 2카드라 우측 하단이 크게 비었다. 보조 기능인 물 섭취를 우측으로 옮겨
   좌우 3카드씩 맞췄다 (PRD 4항 배치도 갱신).

**PRD 변경** — 카테고리 7색 UI 크롬을 폐기하고 일러스트 플레이트로 구분하도록 4항을 다시 썼다.
Airbnb DLS는 secondary brand color를 두지 않으므로 색 7개를 유지하면 브랜드가 깨진다.

---

## 태스크 11 — 레이아웃 재구성 · 운동 종류 · 점수 표정 ✅

**요청 4건**

| # | 요청 | 처리 |
|---|---|---|
| 1 | 상단 인사를 꽉 차게 · 중앙 정렬 · 배경색 채우기 | 화면 전체 폭 밴드로 전환. Airbnb 브랜드 그라데이션(`#E61E4D → #E31C5F → #D70466`) 채우고 인사·날짜·요약 필을 중앙 정렬. 브랜드 마크와 테마 토글도 밴드 안으로 흡수 |
| 2 | 식단·운동·수분·수면 2×2, 메모+체크리스트 하단 중앙 합본 | 좌우 2단 구조를 폐기하고 `.features` 2×2 그리드로 교체. 메모·체크리스트를 카드 하나(`.combo`) 안 2단으로 합쳐 하단 중앙 배치 |
| 3 | 운동을 유산소/웨이트로 구분 · 아이콘 · 각각 시간 기재 | 세그먼트(아이콘+라벨)로 종류 선택, 목록 행에 종류 배지 표시, 총합과 별도로 종류별 합계 표시 |
| 4 | 점수 색을 상단 배경과 통일 · 상/중/하 표정 | 점수 영역을 상단과 같은 밴드로 전환. 총점 구간에 따라 😄 / 😐 / 😢 표시 |

**운동 종류 아이콘**

첫 시안은 러너 실루엣 + 모션 라인이었는데 배지 안 14px에서 형태가 뭉개졌다.
단순 선 아이콘으로 교체 — **유산소 = 심박 파형**, **웨이트 = 덤벨**.

**스키마 변경**

`workouts[].type` 추가 → 저장 키를 `health-dashboard-v1` → `health-dashboard-v2` 로 올리고
마이그레이션을 넣었다 (CLAUDE.md 기술 규칙).

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 운동 종류 구분 저장 | 유산소 러닝 30분 · 웨이트 벤치프레스 15분 → 배지 각각 확인 | PASS |
| 종류 아이콘 표시 | 목록 행 배지 안 `.tico` 2개 | PASS |
| 종류별 합계 | `유산소 30분 · 웨이트 15분` | PASS |
| 삭제 후 종류별 합계 갱신 | 웨이트 삭제 → `유산소 30분` | PASS |
| 점수 표정 — 상 | 8.75점 → 😄 | PASS |
| 점수 표정 — 중 | 5.0점 → 😐 | PASS |
| 점수 표정 — 하 | 5점 미만 → 😢 | PASS |
| 상단 요약 점수 동기화 | 히어로 필 = `오늘 8.8점` | PASS |
| 복구 후 점수 재계산 | 데이터 복원 → 8.8점 | PASS |
| localStorage v2 저장 | v2 키에 저장 · 운동 `type` 포함 | PASS |

**마이그레이션 검증 (별도 실행)**

v1 스키마(운동에 `type` 없음) 데이터를 심고 페이지를 다시 열어 확인했다.

| 검증 항목 | 결과 |
|---|---|
| v2 키로 이관됨 | PASS |
| 운동 2건 보존 | PASS |
| `type` 기본값 `유산소` 채움 | PASS |
| 식단·수분·수면 등 나머지 보존 | PASS |
| 화면에 운동 2건 렌더 | PASS |
| 종류별 합계 표시 | PASS |

**표기 변경**: 카드 제목을 `물 섭취` → `수분 섭취`로 맞추고 점수 항목 라벨도 `물` → `수분`으로 통일.

---

## 미검증 항목

| 항목 | 사유 |
|---|---|
| 날짜 변경 시 초기화(`rolloverIfNewDay`) | 날짜 조작이 필요해 자동 검증에서 제외. 실사용 다음 날 확인 필요 |
| 실제 Edge 창(비헤드리스) 조작 | 검증은 Edge headless + DOM 덤프로 수행. 수동 클릭은 미확인 |

---

## 백로그 (다음 버전 후보)

| 우선순위 | 항목 | 메모 |
|---|---|---|
| 상 | 날짜 변경 초기화 실검증 | 위 미검증 항목 |
| 상 | 백업 내보내기 / 불러오기 | 캐시 삭제 시 데이터 소실 대비 |
| 중 | 최근 7일 점수 추이 | 자정 초기화 시 점수를 아카이브해 미니 막대로 표시 |
| 중 | 어제 기록 보기 | 현재는 자정에 사라짐 |
| 하 | 식단 즐겨찾기 (자주 먹는 메뉴 원클릭) | 입력 마찰 추가 감소 |
| 하 | 목표치 사용자 설정 | 현재 수면 7h / 운동 30분 / 식사 3끼 / 물 8잔 고정 |
| 보류 | 칼로리 자동 계산 | PRD 5항 제외 범위. 음식 DB 필요 |

---

## 검증 실행 방법

> **검증은 실제 Supabase 계정을 건드린다.** 아래 테스트 계정 전용이며, 실사용 계정으로 돌리지 말 것.
> 검증 스크립트에는 비밀번호를 넣지 않는다 — 실행 시 환경변수로 주입한다.

### 0) 테스트 계정 초기화 (매 실행 전 필수)

이전 실행의 데이터가 남아 있으면 개수 검증이 전부 어긋난다. 먼저 SQL을 돌린다.

```sql
with u as (select id from auth.users where email = 'health.dash.test@gmail.com')
, d1 as (delete from public.meals           where user_id in (select id from u))
, d2 as (delete from public.workouts        where user_id in (select id from u))
, d3 as (delete from public.memos           where user_id in (select id from u))
, d4 as (delete from public.daily_logs      where user_id in (select id from u))
, d5 as (delete from public.checklist_marks where user_id in (select id from u))
, d6 as (delete from public.checklist_items where user_id in (select id from u))
insert into public.checklist_items (user_id, text, sort_order)
select u.id, t.text, t.ord
from u, (values ('일어나서 물 한 잔',1),('계단 이용하기',2),
                ('야식 먹지 않기',3),('12시 전에 눕기',4)) as t(text, ord);
```

### 1) 눈으로 확인 (수동)

```powershell
$env:HEALTH_TEST_EMAIL = "health.dash.test@gmail.com"
$env:HEALTH_TEST_PW    = "<비밀번호>"
$src = "헬스케어_대시보드.html"; $inj = "_health_test_inject.js"
$tmp = "$env:TEMP\healthtest"; New-Item -ItemType Directory -Force $tmp | Out-Null
$html = [IO.File]::ReadAllText($src, [Text.Encoding]::UTF8)
$js   = [IO.File]::ReadAllText($inj, [Text.Encoding]::UTF8)
$creds = "window.__TEST_CREDS={email:'$env:HEALTH_TEST_EMAIL',pw:'$env:HEALTH_TEST_PW'};"
[IO.File]::WriteAllText("$tmp\test.html",
  $html.Replace('</body>', "<script>$creds</script><script>$js</script></body>"),
  (New-Object Text.UTF8Encoding($false)))
# 생성된 test.html을 브라우저로 열면 하단에 통과/실패 결과가 표시됨
```

> 재실행 전에는 브라우저 프로필을 지우거나 `localStorage.clear()` 를 먼저 실행할 것.
> 세션 토큰이 남아 있으면 로그인 게이트 검증이 건너뛰어진다.

### 2) 헤드리스 자동 실행 (결과를 텍스트로 회수)

```powershell
$edge = "${env:ProgramFiles(x86)}\Microsoft\Edge\Application\msedge.exe"
$tmp  = "$env:TEMP\healthtest"
$prof = Join-Path $tmp "prof"
if (Test-Path $prof) { Remove-Item -Recurse -Force $prof }   # 프로필 초기화 필수
$url = ([Uri]"$tmp\test.html").AbsoluteUri
Start-Process -FilePath $edge -Wait -NoNewWindow `
  -ArgumentList @("--headless=new","--disable-gpu","--no-first-run",
                  "--allow-file-access-from-files","--user-data-dir=$prof",
                  "--virtual-time-budget=90000","--dump-dom",$url) `
  -RedirectStandardOutput "$tmp\dom.txt" -RedirectStandardError "$tmp\err.txt" | Out-Null
$dom = [IO.File]::ReadAllText("$tmp\dom.txt", [Text.Encoding]::UTF8)
$m = [regex]::Matches($dom, '자동 검증 결과 — [0-9][\s\S]*')
(($m[0].Value -replace '</div>', "`n") -replace '<[^>]+>','').Trim()
```

기능을 수정한 뒤에는 `_health_test_inject.js`에 검증 항목을 추가하고 위 절차를 다시 실행할 것.
현재 항목 수는 **42개**다.

### 3) v1 → v2 마이그레이션 검증 (별도)

스키마 마이그레이션은 페이지를 두 번 열어야 확인되므로 상시 검증 스크립트에 넣지 않았다.
검증이 필요하면 v1 스키마(운동에 `type` 없음) 데이터를 심는 스크립트를 주입한 사본을 만들어
새 브라우저 프로필로 실행하고, 두 번째 로드에서 `health-dashboard-v2` 키와
`workouts[].type === "유산소"` 를 확인한다. (2026-08-12 기준 6 / 6 통과)

---

## 태스크 14 — 로그인 제거 · localStorage 복귀 ✅

**요청**: "그냥 로그인 방법 없애자"

**왜 되돌렸나**

태스크 12에서 넣은 Supabase 인증이 가입 단계에서 막혔다.

| 확인한 것 | 결과 |
|---|---|
| `GET /auth/v1/settings` | `mailer_autoconfirm: false` — 이메일 확인 필수 |
| `POST /auth/v1/signup` | `429 over_email_send_rate_limit` — 무료 플랜 메일 한도 초과 |
| `PATCH api.supabase.com/.../config/auth` | `401 JWT could not be decoded` — publishable 키로는 설정 변경 불가 |

이 설정은 Supabase 대시보드에서만 끌 수 있고 코드로 우회할 수 없다.
개인 1인용 대시보드에 계정·이메일 확인은 과한 마찰이라 PRD 5항 제외 범위대로 되돌렸다.

**제거한 것**

- 로그인 게이트 마크업(`#authGate`, `.authcard`)과 관련 CSS 전부
- 세션 처리 — `storeSession` · `restoreSession` · `normalize` · `freshToken`
- 인증 — `authReq` · `authMessage` · `signIn` · `signUp` · `signOut`
- Supabase REST — `req` · `get` · `insert` · `upsert` · `remove` · `loadAll` · `importLegacy` · `me`
- 헤더의 로그아웃 버튼, `body.locked` · `body.booting` 상태

**남긴 것** — 태스크 11까지의 화면·기능은 그대로다. 디자인·2×2 레이아웃·운동 종류·점수 표정 모두 유지.

**저장 계층 교체 방식**

모든 변경이 `state 갱신 → render() → push(서버요청)` 순서였다.
서버 요청 콜백 10곳을 중괄호 짝을 세어 `save()` 로 치환하고, `save()` 는 `state` 전체를
`localStorage["health-dashboard-v2"]` 에 쓴다. 핸들러 로직은 손대지 않았다.

`health-dashboard-v1` 키가 있으면 읽어서 운동 `type` 을 `유산소` 로 채워 이관한다.

**파일 크기**: 76.7KB → 53.8KB

| 검증 항목 | 결과 |
|---|---|
| 로그인 게이트·로그아웃 버튼 없음, 대시보드 즉시 표시 | PASS |
| 기본 체크 항목 4종 생성 | PASS |
| 식단 기록·삭제 | PASS |
| 운동 종류 배지·종류별 합계·0분 차단·삭제 후 갱신 | PASS |
| 수분 증감, 수면 자동 계산·목표 배지 | PASS |
| 메모 저장·삭제, 체크 토글·추가·삭제 | PASS |
| 점수 8.8 · 표정 😄 · 상단 요약 동기화 | PASS |
| localStorage 저장, 세션 키 미생성 | PASS |

**전체 28 / 28 통과.** 라이트·다크 렌더링 확인.
---

## 태스크 16 — 누적 기록 ✅

**요청**: "사용자별 레코드 기록은 어떻게 하지? 나만의 누적 기록을 못 보잖아"

**원인은 로그인이 아니라 저장 구조였다.**
`state` 가 하루치만 들고 있었고, 날짜가 바뀌면 식단·운동·수분·수면을 그냥 비웠다.
계정이 있든 없든 어제 기록은 남지 않는 구조였다.

**스키마 v2 → v3**

```jsonc
{ "version": 3,
  "days":       { "2026-08-12": { meals, workouts, water, sleep } },
  "checkItems": [ {id, text} ],              // 날짜와 무관한 템플릿
  "checkMarks": { "2026-08-12": [itemId] },  // 그날 완료한 항목
  "memos":      [ {id, text, at} ] }
```

핵심은 **`state` 를 "보고 있는 날짜의 기록"을 가리키는 참조로 바꾼 것**이다.
`state = dayOf(view)` 한 줄로, 15개 핸들러(`state.meals.push(...)` 등)를 하나도
고치지 않고 지난 날 기록·수정이 그대로 동작한다.

**완료 표시를 날짜별로 분리**한 덕에 자정 초기화 로직 자체가 없어졌다.
그 날짜의 `checkMarks` 가 없으면 체크는 저절로 풀린 상태가 된다.

**추가한 화면**

| 요소 | 내용 |
|---|---|
| 날짜 이동 | `◀` / 날짜 / `▶` / `오늘로`. 미래는 이동 불가 |
| 지난 기록 표시등 | 오늘이 아니면 `지난 기록 보는 중` 필 노출 |
| 최근 14일 막대 | 점수 높이 + 평균. 막대를 누르면 그날로 이동 |
| 백업 | 내보내기 / 불러오기 (JSON) |

빈 날짜 칸은 저장 시 자동 정리한다(오늘·보는 날짜 제외). 열어만 보고 안 적은 날이 쌓이지 않게.

| 검증 항목 | 결과 |
|---|---|
| v3 스키마 저장 · 오늘 기록이 날짜 칸에 | PASS |
| 체크 항목 템플릿 / 완료 표시 날짜별 분리 | PASS |
| 메모는 날짜와 무관하게 보관 | PASS |
| 추이 막대 14개 · 오늘 선택 · 평균 표시 | PASS |
| 어제로 이동 · 표시등 · 버튼 활성 상태 | PASS |
| 지난 날에도 기록 가능 | PASS |
| 오늘로 복귀 시 오늘 기록·총점 그대로 | PASS |
| 어제·오늘 두 날짜가 함께 보관됨 | PASS |

**전체 54 / 54 통과.**

**v2 → v3 마이그레이션 (별도 실행) 10 / 10 통과**
하루치 기록을 자기 날짜 칸으로 옮기고, `type` 없던 운동은 유산소로,
`checks[].done` 은 템플릿 + 그날 완료 표시로 분리한다. 메모는 그대로 보존.

> **남은 한계**: 기기 간 동기화는 계정이 필요하다. 백업 파일로 옮기는 것으로 대신한다.
> 추이는 최근 14일만 보여준다 (그 이전도 저장은 돼 있음).
---

## 태스크 18 — 계정 (아이디 + 비밀번호) ✅

**요청**: 이메일 없이 아이디·비밀번호로 계정을 나눠 기록이 섞이지 않게. 그리고 더 귀엽게.

**먼저 확인한 것 — 서버 없이 어디까지 되는가**

| 확인 | 결과 |
|---|---|
| `file://` 이 secure context 인가 | `isSecureContext = true`, `crypto.subtle` 사용 가능 |
| 헤드리스에서 비동기 WebCrypto 가 끝나는가 | **끝나지 않음.** PBKDF2/AES-GCM 프로미스가 미해결로 남음 |

WebCrypto 위에 로그인을 얹으면 **브라우저 검증을 할 수 없다**(PRD 7항 위반).
그래서 **동기 SHA-256 을 직접 구현**해 솔트 + 3000회 반복으로 비밀번호를 해싱한다.

**사용자에게 알린 한계** — 이건 잠금이 아니라 칸막이다.
비밀번호는 평문으로 남지 않지만 기록 자체는 개발자도구로 읽힌다.
사용자가 이 수준을 선택했다.

**스키마 v3 → v5**

계정 배열을 최상위에 두고 각 계정이 자기 `db` 를 갖는다.
`db` 가 활성 계정을 가리키게 해 **아래 코드는 한 줄도 고치지 않았다** (`state = dayOf(view)` 와 같은 수법).

계정을 쓰기 전(v1~v4)에 쌓아 둔 기록은 버리지 않고 **첫 가입자가 물려받는다.**

**로그인 상태는 `sessionStorage`** 에만 둔다. 브라우저를 닫으면 로그아웃된다 —
공용 PC에서 다음 사람에게 그대로 열려 있으면 칸막이의 의미가 없다.

**귀여운 톤** — 로그인 카드에 마스코트(둥실 떠오르는 애니메이션), 28px 라운드,
계정별 동물 이모지(🐰🐻🐱🐨🦊🐣🐼🐯), 계정 칩, 팝인 모션.

| 검증 항목 | 결과 |
|---|---|
| 로그인 전 앱이 가려짐 · 첫 실행은 만들기 모드 | PASS |
| 짧은 비밀번호 거부 (4자 미만) | PASS |
| 계정 생성 후 진입 · 헤더 배지 표시 | PASS |
| 비밀번호를 평문으로 두지 않음 (salt/hash 존재) | PASS |
| 틀린 비밀번호 거부 · 중복 아이디 거부 | PASS |
| 만든 계정이 로그인 화면 칩으로 표시 | PASS |
| **다른 계정은 남의 기록이 안 보임** | PASS |
| 원래 계정 재로그인 시 기록·총점 그대로 | PASS |

**전체 66 / 66 통과.** 라이트·다크 · 360px 모바일 확인.