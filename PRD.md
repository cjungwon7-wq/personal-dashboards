# PRD — 관심종목 모닝 대시보드

- **문서 버전**: v1.0
- **작성일**: 2026-08-12
- **산출물**: `관심종목_대시보드.html` (단일 파일)

---

## 1. 목표

나만의 관심종목 정보를 **한 화면에서** 보는 개인용 대시보드.

## 2. 사용자와 사용 맥락

| 항목 | 내용 |
|---|---|
| 사용자 | 본인 1인 (증권사 신입사원) |
| 사용 시점 | 아침 출근 직후 |
| 사용 시간 | **5분** |
| 사용 환경 | 회사 PC 브라우저 (Edge 기준) |

5분 안에 끝나야 하므로 **입력 마찰을 최소화**하는 것이 설계의 최우선 원칙이다.
페이지를 열면 커서가 종목 입력창에 자동으로 놓이고, 모든 추가 동작은 Enter로 완결된다.

## 3. 핵심 기능

### 3.1 관심종목 등록 · 삭제
- 종목명(필수), 종목코드(선택) 입력으로 등록
- 등록 시 목록 최상단에 추가되고 자동 선택됨
- 종목명 + 코드가 모두 동일하면 중복 등록 차단
- `×` 클릭으로 삭제, 확인창 표시 (메모 보유 시 건수 안내)

### 3.2 메모 기록
- 종목 선택 시 우측에 메모 입력창 표시
- 메모는 종목별로 귀속되며 저장 시각(월/일 시:분)이 자동 기록됨
- 최신 메모가 위로 정렬, 개별 삭제 가능
- `Ctrl(⌘) + Enter` 단축키 저장

### 3.3 오늘의 체크리스트
- 기본 항목 4종 제공 (전일 미국 증시 / 경제지표·일정 / 관심종목 공시 / 오늘 업무 3가지)
- 항목 추가·삭제, 체크 토글
- 진행률 바 및 `n / m` 표시, 전량 완료 시 강조
- **날짜가 바뀌면 체크만 자동 해제되고 항목은 유지됨**

## 4. 화면 구성

```
┌──────────────────────────────────────────────┐
│ 상단 배너 : 시간대별 인사말 · 날짜/요일 · 도구 버튼 │
├────────────────────┬─────────────────────────┤
│ 좌측               │ 우측                     │
│ 관심종목 카드       │ 메모 카드                │
│                    ├─────────────────────────┤
│                    │ 오늘의 체크리스트 카드    │
└────────────────────┴─────────────────────────┘
```

**카테고리 색 구분** (Samsung Blue 계열)

| 영역 | 색상 | 라이트 | 다크 |
|---|---|---|---|
| 관심종목 | 딥네이비 | `#1428A0` | `#5B8DEF` |
| 메모 | 미드블루 | `#1B72E8` | `#47A3F5` |
| 체크리스트 | 그린 | `#0E9F6E` | `#2ECC8F` |

각 카드는 좌측 4px 컬러 바, 제목 옆 컬러 점, 배지·버튼·포커스 링에 동일 색을 적용해 구분한다.

## 5. 제외 범위 (이번 버전에서 만들지 않음)

- 실시간 시세 연동
- 로그인 / 계정
- 모바일 앱

> 정적 HTML에서 시세 API를 직접 호출하면 CORS에 막히고 API 키 관리가 필요하므로 제외했다.

## 6. 기술 제약

- **단일 HTML 파일**, 외부 라이브러리·빌드 도구 없음
- 저장소는 **localStorage** (`morning-dashboard-v1`)
- 서버 없음 → 파일 더블클릭으로 실행
- 라이트/다크 모드 지원 (OS 설정 자동 감지 + 수동 토글)

### 데이터 모델

```jsonc
{
  "stocks": [
    { "id": "…", "name": "삼성전자", "code": "005930",
      "memos": [ { "id": "…", "text": "…", "at": "ISO8601" } ] }
  ],
  "checks":    [ { "id": "…", "text": "…", "done": false } ],
  "checkDate": "2026-08-12",   // 날짜 변경 감지용
  "selected":  "…"             // 선택된 종목 id
}
```

## 7. 검증 기준

> **브라우저에서 3개 기능이 모두 동작하면 완성.**

검증은 `_test_inject.js`를 원본 HTML에 주입한 사본을 브라우저로 실행해 수행한다.
실제 DOM 이벤트를 발생시켜 등록·삭제·메모·체크를 순서대로 실행하고, 화면 하단에 통과 여부를 출력한다.

**현재 상태: 11 / 11 통과 (2026-08-12)**

## 8. 알려진 제약

- 데이터가 브라우저에만 저장되므로 캐시 삭제·시크릿 모드 시 소실 → `백업 내보내기` / `불러오기` 제공
- 시세가 없으므로 가격 판단은 별도 HTS/MTS 병행 필요
- `color-mix()` CSS를 사용하므로 구형 브라우저에서는 색 구분이 정상 표시되지 않음
