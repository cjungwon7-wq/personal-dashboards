# PRD — 오늘의 건강 대시보드

- **문서 버전**: v1.0
- **작성일**: 2026-08-12
- **산출물**: `헬스케어_대시보드.html` (단일 파일)

---

## 1. 목표

하루에 **무엇을 먹고, 얼마나 잤고, 얼마나 운동했고, 물을 얼마나 마셨는지**
한 화면에서 기록하고 점수로 확인하는 개인용 생활습관 대시보드.

## 2. 사용자와 사용 맥락

| 항목 | 내용 |
|---|---|
| 사용자 | 20대 본인 1인 |
| 목적 | 다이어트 · 몸 관리 |
| 사용 시점 | 하루 중 수시로 (식사 후 · 운동 후 · 자기 전) |
| 사용 환경 | PC 브라우저 |

하루에 여러 번 짧게 열기 때문에 **입력 마찰 최소화**가 최우선이다.
페이지를 열면 커서가 식단 입력창에 놓이고, 모든 기록은 Enter 한 번으로 끝난다.
현재 시각에 맞는 끼니(아침/점심/저녁/간식)가 자동으로 선택된다.

## 3. 핵심 기능

### 3.1 식단 기록
- 끼니 선택(아침 · 점심 · 저녁 · 간식) + 먹은 것 입력 → Enter
- 끼니 순서대로 정렬해 표시, 개별 삭제
- 점수 산정에는 **아침 · 점심 · 저녁만** 반영 (간식 제외)

### 3.2 수면 기록
- 취침 시각 · 기상 시각 입력 → 수면 시간 자동 계산
- 자정을 넘기는 경우(예: 23:40 → 06:30)도 정확히 계산
- 목표(7시간) 달성 시 배지 표시

### 3.3 운동 기록
- **종류를 유산소 / 웨이트로 구분**해서 기록한다. 종류는 아이콘 + 라벨 세그먼트로 고른다
  - 유산소 = 심박 파형 아이콘, 웨이트 = 덤벨 아이콘 (14px에서도 형태가 유지되는 단순 선 아이콘)
- 운동 이름 + 시간(분) 입력 → Enter
- 목록은 종류 순으로 정렬되고, 각 행에 종류 배지(아이콘+텍스트)와 시간이 함께 표시된다
- 총 운동 시간과 **종류별 합계**(`유산소 25분 · 웨이트 20분`)를 함께 표시
- 0분 이하 입력은 기록되지 않음
- 점수 산정에는 종류 구분 없이 **총 운동 시간**을 쓴다

### 3.4 물 섭취 (보조)
- `+` / `−` 버튼 또는 잔 아이콘 직접 클릭
- 0잔 미만으로 내려가지 않음, 목표 8잔

### 3.4-1 상단 인사
- 시간대별 인사말에 **이모지**를 함께 표시한다 (새벽 🌙 / 아침 ☀️ / 점심 🥗 / 오후 🌤️ / 저녁 🌆 / 밤 🌙)
- 날짜·요일과 오늘 점수 요약 필을 함께 노출

### 3.4-2 누적 기록 (날짜별 보관 · 추이 · 조회)
- 기록은 **날짜별로 쌓인다.** 자정이 지나도 지난 날 기록이 사라지지 않는다
- 상단에 **날짜 이동**(`◀` / `▶` / `오늘로`). 미래 날짜로는 이동할 수 없다
- 지난 날을 보는 중이면 `지난 기록 보는 중` 표시등이 켜진다
- 지난 날에도 **기록을 추가·수정할 수 있다** (깜빡한 날 채워 넣기)
- 하단에 **최근 14일 점수 막대**와 평균 점수. 막대를 누르면 그날로 이동한다
- 상단 도구에 **백업 내보내기 / 불러오기** (JSON). 캐시 삭제·기기 이동 대비

### 3.5 메모 · 체크리스트 (우측)
- 메모: 컨디션 · 식욕 · 몸 상태 기록, `Ctrl(⌘) + Enter` 저장, 저장 시각 기록, 개별 삭제
- 체크리스트: 기본 항목 4종 제공, 추가 · 삭제 · 토글, 진행률 바
- **날짜가 바뀌면 체크만 자동 해제되고 항목은 유지됨**

### 3.6 오늘의 점수 (하단)
네 항목을 목표 대비 비율(최대 1)로 환산해 **각 2.5점씩 배분**, 합계 10점 만점.

| 항목 | 목표 | 배점 |
|---|---|---|
| 수면 | 7시간 | 2.5 |
| 운동 | 30분 (유산소+웨이트 합계) | 2.5 |
| 식단 | 3끼 (아침·점심·저녁) | 2.5 |
| 수분 | 8잔 | 2.5 |

목표를 초과해도 항목별 점수는 2.5점에서 멈춘다(초과 감점 없음).

**표정 표시** — 총점 구간에 따라 얼굴이 함께 바뀐다.

| 구간 | 표정 | 코멘트 |
|---|---|---|
| 8.0 ~ 10 (상) | 😄 | 훌륭해요. 오늘 루틴 그대로 가면 됩니다. |
| 5.0 ~ 7.9 (중) | 😐 | 나쁘지 않아요. 부족한 항목 하나만 채워 보세요. |
| 0 ~ 4.9 (하) | 😢 | 아직 여유가 있어요. 물 한 잔부터 시작해 볼까요? |

점수는 상단 인사 밴드의 요약 필(`오늘 9.0점`)과 항상 같은 값을 보여준다.

## 4. 화면 구성

```
╔══════════════════════════════════════════════╗
║ 상단 밴드 (화면 꽉 참 · 중앙 정렬)             ║
║   브랜드 마크 · 테마 토글                      ║
║   😀 이모지 인사 / 날짜·요일 / 요약 필          ║
╚══════════════════════════════════════════════╝
┌─────────────────────┬────────────────────────┐
│ 식단 기록            │ 운동 기록               │
├─────────────────────┼────────────────────────┤
│ 수분 섭취            │ 수면 기록               │
└─────────────────────┴────────────────────────┘
┌──────────────────────────────────────────────┐
│ 오늘의 체크리스트  │  메모   (한 카드 · 2단)  │
└──────────────────────────────────────────────┘
╔══════════════════════════════════════════════╗
║ 하단 밴드 (상단과 동일 배경)                   ║
║   😄 표정 / 9.0 / 10점 만점 / 항목별 막대       ║
╚══════════════════════════════════════════════╝
```

- **상단 인사**는 화면 전체 폭을 채우는 밴드로, 내용은 중앙 정렬한다.
- **핵심기능 4종**(식단 · 운동 · 수분 · 수면)은 중앙부에 **2×2 그리드**로 배치한다.
  880px 이하에서 1단으로 전환.
- **체크리스트와 메모는 한 카드로 합쳐** 하단 중앙에 둔다. 왼쪽 체크리스트, 오른쪽 메모. 카드 안에서 2단(880px 이하 1단).
- **하단 점수 밴드**는 상단 인사와 같은 배경을 써서 화면 위아래를 같은 색으로 묶는다.

**디자인 언어 — Airbnb** (기준 문서: `DESIGN-airbnb.md`)

핵심 원칙은 **흰 캔버스 + ink + Rausch 단일 액센트**다.
DLS 원문대로 "secondary brand color는 없고, 시각적 무게는 사진과 여백이 진다".

| 역할 | 토큰 | 값 |
|---|---|---|
| Rausch (단일 액센트) | `primary` | `#FF385C` (press `#E00B41`) |
| Ink (본문·수치) | `ink` | `#222222` |
| Body / Muted | `body` / `muted` | `#3F3F3F` / `#6A6A6A` |
| Hairline | `hairline` / `hairline-soft` | `#DDDDDD` / `#EBEBEB` |
| Canvas / Soft / Strong | `canvas` / `surface-soft` / `surface-strong` | `#FFFFFF` / `#F7F7F7` / `#F2F2F2` |

- **타이포**: Cereal VF 대체로 Inter → 시스템 스택. 인사말 28px/700, 카드 제목 20px/600,
  본문 16px/400, 메타 14px/400, 배지 11px/600.
- **형태**: 버튼 8px, 카드 14px, 필·잔·스테퍼는 완전 원형. 하드 코너 없음.
- **엘리베이션**: DLS대로 **그림자 단 한 단계**(`--lift`)만 두고 카드 hover에만 쓴다.
- **입력**: 포커스 시 글로우 링 없이 테두리를 ink 2px로 두껍게만 한다.
- **Rausch 사용처**: 브랜드 마크, 기본 CTA(기록 / 추가 / 메모 저장), 체크리스트 전량 완료 시.
  그 외 수치·막대·잔은 전부 ink — DLS가 별점을 금색이 아닌 ink로 쓰는 것과 같은 이유다.
- **오늘의 점수**는 DLS의 `rating-display`(64px/700 + 월계수 장식)를 그대로 차용한다.
  시스템에서 타이포가 유일하게 커지는 지점이다.

**카테고리 구분 — 색에서 이미지로**

> **PRD v1.0에서 변경.** 초판은 카테고리 7색을 UI 크롬(컬러 바·컬러 버튼)에 썼으나,
> Airbnb DLS의 단일 액센트 원칙과 정면으로 충돌한다. 색 대신 **일러스트 플레이트**로 구분한다.

| 영역 | 구분 수단 | 이미지 톤 |
|---|---|---|
| 식단 | 일러스트 플레이트 | 웜 피치 `#FFF3E2 → #FFD8C0` (샐러드 볼·포크·컵) |
| 수면 | 일러스트 플레이트 | 라벤더 나이트 `#EFEBFF → #C8C4F2` (초승달·별·침대) |
| 운동 | 일러스트 플레이트 | 코럴 `#FFE9ED → #FFC2CE` (러너·덤벨·모션 라인) |
| 물 | 일러스트 플레이트 | 아쿠아 `#E6F6FC → #BCE5F3` (물잔·물방울) |
| 메모 | 라인 글리프 (ink 22px) | — |
| 체크리스트 | 라인 글리프 (ink 22px) | — |
| 오늘의 점수 | 월계수 장식 + 64px 수치 | — |

일러스트는 **인라인 SVG**다. 외부 리소스 금지 규칙(6항) 때문에 실사진 대신 쓰며,
네트워크 요청이 없고 단일 파일 안에 그대로 담긴다.

**다크 모드**

Airbnb 공개 웹에는 다크 모드가 없다. 프로젝트 규칙(라이트/다크 병행)을 지키기 위해
**DLS를 ink 캔버스로 뒤집은 확장 팔레트**를 별도로 정의한다 — canvas `#161616`,
surface `#1E1E1E`, hairline `#343434`, Rausch는 어두운 배경 대비를 위해 `#FF5A75`.
일러스트 플레이트는 파스텔 그대로 두어 사진처럼 떠 보이게 한다.

## 5. 제외 범위 (이번 버전에서 만들지 않음)

- **로그인 / 계정** (v2에서 Supabase 인증을 넣었다가 되돌림 — 8항 참조)
- 모바일 앱
- 칼로리 자동 계산, 음식 DB 연동
- 주간 · 월간 추이 그래프

## 6. 기술 제약

- **단일 HTML 파일**, 외부 라이브러리·빌드 도구·CDN 없음
- 저장소는 **localStorage** (`health-dashboard-v2`) — 서버도 계정도 없다
- 파일 더블클릭(`file://`)으로 그대로 실행된다
- 라이트/다크 모드 지원 (OS 설정 자동 감지 + 수동 토글)
- **배포**: Vercel 정적 호스팅. `vercel.json` 이 `/` → `헬스케어_대시보드.html` 로 라우팅한다.
  배포하더라도 기록은 각자 브라우저에만 남는다 (기기 간 동기화 없음)
### 데이터 모델 (v3)

```jsonc
{
  "version": 3,
  "days": {                       // 날짜별로 쌓인다. 지우지 않는다.
    "2026-08-12": {
      "meals":    [ { "id": "…", "slot": "아침", "text": "그릭요거트", "at": "ISO8601" } ],
      "workouts": [ { "id": "…", "type": "유산소", "name": "러닝", "min": 25 } ],
      "water":    5,
      "sleep":    { "bed": "23:40", "wake": "06:30" }
    }
  },
  "checkItems": [ { "id": "…", "text": "일어나서 물 한 잔" } ],   // 날짜와 무관한 템플릿
  "checkMarks": { "2026-08-12": ["itemId1", "itemId2"] },        // 그날 완료한 항목
  "memos":      [ { "id": "…", "text": "…", "at": "ISO8601" } ]  // 날짜와 무관
}
```

`type` 은 `"유산소"` 또는 `"웨이트"`.

**완료 표시를 날짜별로 분리한 덕에 자정 초기화 로직이 필요 없다.**
그 날짜의 `checkMarks` 항목이 없으면 체크는 저절로 풀린 상태가 된다.

기록이 하나도 없는 날짜 칸은 저장할 때 자동으로 정리한다(오늘과 보고 있는 날짜는 제외).
열어만 보고 아무것도 적지 않은 날이 쌓이지 않게 하기 위해서다.

### 마이그레이션

| 원본 | 처리 |
|---|---|
| `health-dashboard-v2` / `v1` | 하루치만 있던 기록을 `days[그 날짜]` 칸으로 옮긴다 |
| 운동 `type` 없음 | `유산소` 로 채운다 |
| `checks[].done` | 항목은 `checkItems` 로, 완료 여부는 `checkMarks[그 날짜]` 로 분리 |
| 메모 | `memos` 로 그대로 옮긴다 |
## 7. 검증 기준

> **브라우저에서 3개 핵심 기능(식단 · 수면 · 운동)이 모두 동작하면 완성.**

검증은 `_health_test_inject.js`를 원본 HTML에 주입한 사본을 브라우저로 실행해 수행한다.
실제 DOM 이벤트를 발생시켜 기록·삭제·계산·점수를 순서대로 실행하고, 화면 하단에 통과 여부를 출력한다.

**현재 상태: 54 / 54 통과 + v2→v3 마이그레이션 10 / 10 통과 (2026-08-12, Edge headless)**

## 8. 알려진 제약

- 기록이 **이 브라우저에만** 남는다. 캐시를 지우거나 시크릿 모드로 열면 사라진다
- 기기 간 동기화가 없다. 회사 PC와 집 PC의 기록은 서로 다르다
- 페이지를 열어 둔 채 자정을 넘기면 새로고침 전까지 날짜가 넘어가지 않는다
- 추이는 **최근 14일**만 보여준다. 그 이전 기록은 저장돼 있지만 화면에서 조회할 방법이 없다
- localStorage 용량 한도(보통 5MB)에 걸리면 저장이 실패한다. 하루치가 1KB 미만이라 수년치는 여유롭다
- 수면은 7시간을 넘겨도 감점하지 않는다 (과다 수면 미반영)
- `color-mix()` · `aspect-ratio` CSS를 쓰므로 구형 브라우저에서는 레이아웃이 깨질 수 있다

### 로그인을 뺀 이유 (2026-08-12)

v2에서 Supabase Auth + 7개 테이블로 계정별 저장을 붙였으나 되돌렸다.

- Supabase 기본 설정이 **가입 시 이메일 확인을 요구**한다. 확인 메일 없이는 세션이 발급되지 않는다
- 무료 플랜 메일 발송 한도가 낮아 가입 시도가 `429 over_email_send_rate_limit` 로 막혔다
- 이 설정은 **대시보드에서만** 끌 수 있고 코드로는 우회할 수 없다 (publishable 키로 설정 변경 시 401)
- 개인 1인용 대시보드에 계정·이메일 확인은 과한 마찰이라, PRD 5항 제외 범위대로 되돌렸다

되살릴 때는 Supabase 대시보드에서 `Authentication → Sign In / Providers → Email → Confirm email`
을 먼저 끄고 시작할 것. 스키마와 RLS 정책은 git 히스토리(태스크 12)에 남아 있다.