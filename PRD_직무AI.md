# PRD — 인사노무 법령 브리핑 (직무AI 대시보드)

- **문서 버전**: v0.1 (초안 — 승인 전)
- **작성일**: 2026-08-13
- **산출물**: `직무AI_대시보드.html` + `api/` 서버 함수
- **상태**: ⏳ 승인 대기. 8항의 미결 항목을 확정해야 태스크 착수 가능

---

## 1. 목표

인사노무 담당자가 **매일 아침 5분 안에** 노동법 관련 변화를 확인하는 사내 브리핑 화면.

확인 대상은 네 가지다.

1. 노동법 및 관계법령의 제·개정
2. 노동법 관련 정부 주요 고시
3. 노동법 관련 주요 판례
4. 노동법 관련 주요 행정해석

**핵심 가치는 "찾아다니지 않아도 된다"는 것이다.** 담당자가 매일 국가법령정보센터·고용노동부·대법원을
각각 열어보는 수고를 없애는 것이 목적이며, 이를 위해 **수집은 자동으로** 이뤄져야 한다.
누군가 매일 손으로 채워야 하는 구조라면 이 사이트는 존재 이유가 없다.

## 2. 사용자와 사용 맥락

| 항목 | 내용 |
|---|---|
| 주 사용자 | 사기업 HR ER (인사노무) 담당자 |
| 보는 사람 | **인사팀 전원** (읽기) |
| 채우는 사람 | 담당자 1~2명 (큐레이션·코멘트) |
| 사용 시점 | 매일 아침 |
| 갱신 주기 | 일별 (주별 요약 병행) |
| 사용 환경 | 회사 PC 브라우저 |

**읽는 사람과 쓰는 사람이 다르다.** 이 구분이 권한 설계의 기준이다.

**로그인은 선택이다** (2026-08-13 변경 — 초판은 전원에게 요구했다).
경계는 로그인 여부가 아니라 **무엇이 사내 정보인가**로 긋는다.

| 대상 | 로그인 | 이유 |
|---|---|---|
| 법령·고시·판례·행정해석·HR 이슈 | 불필요 | 법제처·대법원·언론의 **공개 자료**다. 로그인을 요구할 근거가 없고, 아침 5분에 마찰만 더한다 |
| 담당자 노트 · 큐레이션 코멘트 | **필요** | 사내 정보다. 초판이 로그인을 요구한 이유가 바로 이것이다 |

권한은 세 단계가 된다: 비로그인(브리핑 열람) / 팀원(노트 읽기) / 담당자(노트·큐레이션 작성).

> 이 경계는 화면이 아니라 **RLS 가 긋는다.** 비로그인 요청에는 노트가 한 줄도 내려오지 않는다.
> 화면에서 감추기만 하면 칸막이가 되지 않는다.

계정은 **관리자가 발급한다.** 자유 가입은 사내 문서 성격상 부적절하다.

기존 두 프로젝트(관심종목·헬스케어)와 달리 **1인용이 아니다.** 한 사람이 올린 내용을
팀 전원이 봐야 하므로 브라우저 로컬 저장으로는 성립하지 않는다.

## 3. 핵심 기능

### 3.1 자동 수집 (서버)

법제처 **국가법령정보 공동활용** OPEN API를 서버에서 호출해 매일 새벽 수집한다.

| 카드 | API target | 조회 기준 |
|---|---|---|
| 법령 제·개정 | `law` | 시행일·공포일 기준 신규분 |
| 정부 주요 고시 | `admrul` (행정규칙 — 고시·훈령·예규) | 발령일 기준 신규분 |
| 주요 판례 | `prec` | `prncYd` 선고일자 범위 |
| 주요 행정해석 | 고용노동부 법령해석 | 해석일자 기준 신규분 |

**브라우저에서 직접 호출하지 않는다.** 세 가지 이유가 모두 서버 중계를 요구한다.

| 제약 | 내용 |
|---|---|
| 엔드포인트가 `http://` | `http://www.law.go.kr/DRF/lawSearch.do` — HTTPS 페이지에서 mixed content로 차단됨 |
| CORS | 외부 도메인 호출을 허용하지 않음 |
| `OC` 인증값 | 정적 파일에 넣으면 누구나 볼 수 있어 노출됨 |

`OC` 키는 Vercel 환경변수에 두고 서버 함수만 읽는다. 클라이언트로 내려보내지 않는다.

**감시 대상 법령 16개** (2026-08-13 확정)

| # | 법령 | 실무 접점 |
|---|---|---|
| 1 | 근로기준법 | 근로시간·임금·해고·휴가·직장 내 괴롭힘 |
| 2 | 최저임금법 | 매년 고시. 임금 설계 직결 |
| 3 | 근로자퇴직급여 보장법 | 퇴직금·퇴직연금(DB/DC) |
| 4 | 산업안전보건법 | 안전보건 의무·과태료 |
| 5 | 중대재해처벌법 | 경영책임자 처벌. 리스크 최상위 |
| 6 | 파견근로자 보호 등에 관한 법률 | 파견·도급 구분(불법파견) |
| 7 | 기간제 및 단시간근로자 보호 등에 관한 법률 | 계약직 2년 제한·차별 시정 |
| 8 | 남녀고용평등과 일·가정 양립 지원에 관한 법률 | 육아휴직·직장 내 성희롱 |
| 9 | 노동조합 및 노동관계조정법 | 단체교섭·쟁의행위 |
| 10 | 근로자참여 및 협력증진에 관한 법률 | 노사협의회 |
| 11 | 고용보험법 | 실업급여·고용유지지원금 |
| 12 | 산업재해보상보험법 | 산재 처리 |
| 13 | 임금채권보장법 | 도산 시 대지급금 |
| 14 | 장애인고용촉진 및 직업재활법 | 의무고용률·고용부담금 (매년 고시) |
| 15 | 고용상 연령차별금지 및 고령자고용촉진에 관한 법률 | 정년·계속고용 |
| 16 | 채용절차의 공정화에 관한 법률 | 채용 실무 |

각 법령의 **시행령·시행규칙도 함께 감시한다.** 실무를 바꾸는 것은 대개 하위법령이다.

검토 후 제외한 후보: 외국인근로자의 고용 등에 관한 법률 · 근로복지기본법 · 개인정보보호법.
필요해지면 목록에 추가하면 되고, 수집 로직은 바꿀 필요가 없다.

### 3.2 네 카드 열람

- 각 카드에 최신 항목이 날짜 내림차순으로 표시된다
- 각 항목은 **제목 · 날짜 · 출처 · 원문 링크**를 반드시 갖는다
- 어제 이후 새로 들어온 항목에는 `NEW` 표시
- 항목을 열면 요약과 원문 링크가 보인다

### 3.3 담당자 큐레이션 (로그인 필요)

- 항목에 **중요 표시**를 달아 상단에 고정
- 항목에 **해설 코멘트**를 달아 팀에 전달 (예: "우리 사 교대제와 연관 있음")
- 관련 없는 항목 숨기기

### 3.4 산업군 필터

산업군(금융 · 제조 · IT · 유통)을 고르면 해당 산업과 관련된 항목이 먼저 보인다.

**별도 데이터 소스가 아니다.** 수집한 항목에 키워드로 산업 태그를 붙여 거르는 방식이다.

| 산업군 | 태그 키워드 (초안) |
|---|---|
| 금융 | 근로시간 특례 · 성과급 · 퇴직연금 · 영업점 교대제 |
| 제조 | 산업안전보건법 · 중대재해처벌법 · 연장근로 · 교대제 |
| IT | 포괄임금 · 유연근무 · 재량근로 · 스톡옵션 |
| 유통 | 감정노동 · 휴게시간 · 단시간근로 · 주휴수당 |

### 3.5 담당자 노트 (하단, 로그인 필요)

**팀에 전하는 코멘트**다. 개인 메모가 아니라 전원이 읽는 공지 성격이다.
작성자·작성 시각이 함께 남고, 작성자만 수정·삭제할 수 있다.

### 3.6 AI 요약 (마지막 단계)

수집한 원문은 길어서 아침 5분에 읽기 어렵다. 3줄 요약을 붙인다.

**정확성 원칙 — 타협하지 않는다.**

| 원칙 | 이유 |
|---|---|
| 원문 링크를 항상 함께 둔다 | 판단 근거는 요약이 아니라 원문이어야 한다 |
| AI 요약에는 `AI 요약` 라벨을 붙인다 | 사람이 쓴 코멘트와 구별되어야 한다 |
| 요약은 "읽을지 고르는 용도"로만 안내한다 | 잘못 요약된 판례를 근거로 판단하면 실무에 직접 피해가 간다 |
| 법령 조문·판시사항을 요약이 창작하지 않게 한다 | 원문에 없는 내용을 만들면 안 된다 |

이 사이트는 **법률 자문이 아니다.** 화면에 이 문구를 상시 노출한다.

## 4. 화면 구성

```
╔══════════════════════════════════════════════╗
║  2026-08-13 (목) 노동법 이슈 확인              ║
║  산업군: [금융] [제조] [IT] [유통]   마지막 수집 06:00 ║
╚══════════════════════════════════════════════╝
┌──────────────────────┬───────────────────────┐
│ 법령 제·개정          │ 정부 주요 고시         │
├──────────────────────┼───────────────────────┤
│ 주요 판례             │ 주요 행정해석          │
└──────────────────────┴───────────────────────┘
┌──────────────────────────────────────────────┐
│ 담당자 노트 (팀 공지)                          │
└──────────────────────────────────────────────┘
```

- 상단은 화면 전체 폭 밴드. 날짜·산업군 필터·마지막 수집 시각을 둔다
- 네 카드는 **2×2 그리드**. 880px 이하에서 1단 전환
- 담당자 노트는 하단 전체 폭
- 라이트/다크 모드 병행

**디자인 언어 — Clay** (기준 문서: `DESIGN-clay.md`)

핵심은 **크림 캔버스 + 니어블랙 CTA + 채도 높은 피처 카드**다.
Clay 는 Airbnb DLS 와 정반대로 **다색 시스템**이라, 4카드를 색으로 구분하는 것이 스펙에 부합한다.

| 역할 | 토큰 | 값 |
|---|---|---|
| Canvas (크림 — 비타협) | `canvas` | `#fffaf0` |
| Surface Soft / Card | `surface-soft` / `surface-card` | `#faf5e8` / `#f5f0e0` |
| Primary (CTA·헤드라인) | `primary` / `primary-active` | `#0a0a0a` / `#1f1f1f` |
| Ink / Body / Muted | `ink` / `body` / `muted` | `#0a0a0a` / `#3a3a3a` / `#6a6a6a` |
| Hairline | `hairline` | `#e5e5e5` |

**카드 색 배정** (2×2 그리드 — 네 카드 모두 다른 색. 스펙의 "같은 색 연달아 금지")

| 카드 | 색 | 글자 | 대비 |
|---|---|---|---|
| 법령 제·개정 | `brand-teal` `#1a3a3a` | 흰색 | 12.3 : 1 |
| 정부 주요 고시 | `brand-ochre` `#e8b94a` | ink | 10.8 : 1 |
| 주요 판례 | `brand-lavender` `#b8a4ed` | ink | 9.0 : 1 |
| 주요 행정해석 | `brand-peach` `#ffb084` | ink | 11.1 : 1 |
| 담당자 노트 | `surface-card` `#f5f0e0` (크림) | ink | — |

> **`brand-pink` `#ff4d8b` 는 쓰지 않는다.** 스펙은 핑크 카드에 흰 글씨를 지정하지만
> 실측 대비가 **3.1 : 1** 로 본문 기준(4.5)에 미달한다. 매일 아침 읽는 목록에는 부적합하다.
> 큰 제목에는 3:1 을 통과하므로, 필요하면 제목 한 곳에만 제한적으로 쓴다.

- **타이포**: Plain Black 은 라이선스 폰트라 사용 불가. 스펙 권고대로 **Inter 500 + 음수 자간**
  으로 대체하고, CDN 금지 규칙 때문에 시스템 스택으로 폴백한다.
  카드 제목 `title-md` 18/600, 본문 `body-md` 16/400, 메타 `body-sm` 14/400, 배지 `caption` 13/500.
- **형태**: 버튼·입력 12px, 콘텐츠 카드 16px, **피처 카드 24px**, 배지·탭은 완전 원형.
- **엘리베이션**: 무거운 그림자를 쓰지 않는다. 깊이는 크림 캔버스와 채도 카드의 색 대비가 만든다.
- **여백** — 스펙에서 **한 단계 줄인다** (PRD 2항 아침 5분과 충돌하기 때문):

| 항목 | Clay 스펙 | 직무AI |
|---|---|---|
| 섹션 리듬 | 96px | **48px** |
| 피처 카드 패딩 | 32px | **24px** |

  색·타이포·radius 는 Clay 값을 그대로 쓴다. 줄이는 것은 여백뿐이다.

**다크 모드**

Clay 공개 사이트에는 다크 모드가 없다("cream-throughout is a system contract").
프로젝트 규칙(라이트/다크 병행)을 지키기 위해 **Clay 자체 토큰으로 확장 팔레트를 정의한다** —
없는 색을 지어내지 않는다.

| 역할 | 다크 값 | 출처 |
|---|---|---|
| Canvas | `#0a1a1a` | Clay `surface-dark` |
| Surface | `#1a2a2a` | Clay `surface-dark-elevated` |
| 본문 / 보조 | `#ffffff` / `#a0a0a0` | Clay `on-dark` / `on-dark-soft` |
| Hairline | `color-mix(in srgb, on-dark 14%, surface-dark-elevated)` | 파생값 |

**채도 카드 4색은 다크에서도 그대로 둔다.** 브랜드의 핵심이고, 위 표의 대비가 배경과 무관하게 유지된다.

**일러스트** — Clay 의 3D 클레이메이션은 커미션 에셋이라 재현할 수 없다.
외부 리소스 금지 규칙(6항)까지 겹치므로 **인라인 SVG 로 근사**하되, 스펙의
"flat vector 로 대체 금지" 원칙을 완전히 만족시키지 못한다는 점을 한계로 남긴다.
법령 브리핑이라 일러스트 비중 자체를 낮게 잡는다.

## 5. 제외 범위 (이번 버전에서 만들지 않음)

- 법률 자문·해석 제공 (원문 전달과 요약까지만)
- 사내 인사 데이터 연동 (급여·근태)
- 알림 발송 (이메일·슬랙)
- 판례 전문 검색 엔진
- 모바일 앱

## 6. 기술 구조

**단일 HTML 파일 규칙의 예외 프로젝트다.** 자동 수집·팀 공유가 서버를 요구한다.

```
브라우저 (직무AI_대시보드.html)
      │
      ├─→ Supabase (읽기: 로그인 · 쓰기: 담당자)  ← 수집 결과 · 큐레이션 · 노트
      │
      └   Vercel Cron → api/collect.js → 법제처 OPEN API
                            └→ Supabase 저장
```

| 항목 | 선택 |
|---|---|
| 화면 | 순수 HTML/CSS/JS 단일 파일 유지 (프레임워크·CDN 없음) |
| 서버 | Vercel 서버리스 함수 (`api/`). 순수 JS, 빌드 도구 없음 |
| DB | Supabase `26 삼성증권 입문과정` (`qplxvfwuobvnobqpivbm`, 서울). 전 테이블 RLS |
| 수집 스케줄 | Vercel Cron (매일 새벽) |
| 비밀값 | `OC` 키·service 키는 Vercel 환경변수. 코드·클라이언트에 넣지 않음 |
| 배포 경로 | `vercel.json` 에 `/labor` 추가 (`/` 는 헬스케어 유지) |

기존 두 프로젝트의 단일 파일 규칙은 **그대로 유지된다.** 이 프로젝트만 예외다.

### 테이블 (초안)

같은 Supabase 프로젝트에 헬스케어 태스크 12(되돌림)의 잔여 테이블 7개가 남아 있다 —
`profiles` · `daily_logs` · `meals` · `workouts` · `memos` · `checklist_items` · `checklist_marks`.
**건드리지 않는다.** 이름 충돌을 피하기 위해 직무AI 테이블은 `labor_` 접두어를 쓴다.

| 테이블 | 내용 | 읽기 | 쓰기 |
|---|---|---|---|
| `labor_items` | 수집 항목 (종류·제목·날짜·출처·원문 URL·요약·산업 태그·배지·정렬 가중치) | **공개** | 서버만 |
| `labor_runs` | 수집 실행 기록 (시각·건수·성공 여부) | **공개** | 서버만 |
| `labor_curations` | 중요 표시 · 해설 코멘트 · 숨김 | 멤버 | 담당자 |
| `labor_notes` | 담당자 노트 (팀 공지) | 멤버 | 담당자 (작성자만 수정·삭제) |
| `labor_members` | 역할 (`담당자` / `팀원`) | 본인 | 관리자 |

**RLS 는 전 테이블에 켠다.** 공개 자료(`labor_items` · `labor_runs`)만 `anon` 에 열고,
사내 정보(`labor_notes` · `labor_curations`)는 열지 않는다.
화면에서 감추는 것만으로는 칸막이가 되지 않기 때문에, 경계를 서버에 둔다.

사내 정보의 읽기 자격은 **`authenticated` 가 아니라 `labor_members` 에 등록된 사람**이다
(`private.is_labor_member()`). 저장소를 공개로 전환하면서 publishable 키와 프로젝트 URL 이
함께 공개됐는데, `authenticated` 만으로 열어 두면 **자유 가입으로 계정을 만든 외부인이
그대로 읽을 수 있기 때문이다.** `mailer_autoconfirm: false` 는 방어가 되지 않는다 —
확인 메일은 가입자 본인 주소로 가므로 자기 메일로 가입하면 통과한다.

계정이 생기는 것과 데이터를 읽는 것을 따로 막아, 대시보드 설정(`disable_signup`)을
깜빡해도 방어선이 하나 남게 했다.

`종류` 는 `법령` · `고시` · `판례` · `행정해석` 네 값이며 카드 배치와 1:1 대응한다.
여기에 **`HR이슈`** 가 더해진다 — 2×2 카드 아래 산업별 HR 이슈 목록이 쓰는 값이다.
별도 테이블로 두지 않는 이유는 `labor_curations` 의 FK 가 `labor_items` 를 가리키기 때문이다.
쪼개면 HR 이슈에는 중요 표시를 달 수 없게 된다.

`labor_items` 는 화면이 요구하는 세 가지를 더 갖는다 (태스크 3에서 추가).

| 컬럼 | 용도 |
|---|---|
| `tag` | 항목 배지 — 개정 · 공포 · 고시 · 전합 · 판결 · 해석 |
| `near` · `risk` · `scale` (0~3) | HR 이슈 정렬 가중치. 상단 순서 = `near*4 + risk*2 + scale` |
| `is_sample` | 사람이 넣은 예시 표시. 화면에 `예시` 배지로 드러낸다 |

**`is_sample` 은 장식이 아니다.** 확인된 자료와 자리표시자가 한 목록에 섞이면
어느 쪽을 근거로 쓸 수 있는지 구별되지 않는다. 수집(태스크 5) 항목은 `false` 다.

### 권한 모델

| 동작 | 비로그인 | 팀원 | 담당자 | 서버 |
|---|---|---|---|---|
| 네 카드 열람 · 산업군 필터 · HR 이슈 | ✅ | ✅ | ✅ | — |
| 마지막 수집 시각 | ✅ | ✅ | ✅ | — |
| 담당자 노트 읽기 | ❌ | ✅ | ✅ | — |
| 중요 표시 · 코멘트 · 숨기기 | ❌ | ❌ | ✅ | — |
| 담당자 노트 작성 | ❌ | ❌ | ✅ (작성자만 수정·삭제) | — |
| 수집 실행 | ❌ | ❌ | ❌ | ✅ (Cron) |

**브리핑은 로그인 없이 보이고, 사내 정보에만 로그인이 필요하다.** 계정은 관리자가 발급한다.

읽기 자격은 `authenticated` 가 아니라 **`labor_members` 에 등록된 사람**이다
(`private.is_labor_member()`). 저장소가 공개라 publishable 키가 노출돼 있고 자유 가입이
열려 있어, `authenticated` 로만 열면 외부인이 가입해서 노트를 읽을 수 있다.

### 계정 발급 — 이메일 확인 문제를 지나지 않는다

이 Supabase 프로젝트는 `mailer_autoconfirm: false` 다 (2026-08-13 확인).
가입 API를 쓰면 확인 메일이 필요하고, 헬스케어 태스크 12가 정확히 여기서 막혔다.

**관리자 발급 방식은 그 경로를 지나지 않는다.** 관리자가 Supabase 대시보드에서
`Authentication → Users → Add user` 로 `Auto Confirm User` 를 켜고 만들면
메일 발송 없이 즉시 로그인 가능한 계정이 생긴다.

화면에는 **가입 기능을 만들지 않는다.** 로그인만 있다.
`disable_signup` 도 대시보드에서 꺼서 API 자유 가입 경로를 닫는다 (9항 사용자 조치).

## 7. 검증 기준

기존 두 프로젝트와 같은 방식 — `_job_test_inject.js` 를 주입한 사본을 브라우저로 실행해
실제 DOM 이벤트로 확인하고 화면 하단에 통과 여부를 출력한다.

여기에 **서버 검증이 추가된다.**

| 대상 | 방법 |
|---|---|
| 화면·상호작용 | 브라우저 주입 검증 (기존 방식) |
| 수집 함수 | 실제 법제처 API 호출 → 응답 파싱 결과 확인 |
| 권한(RLS) | 로그인 없이 쓰기 시도가 거부되는지 확인 |
| 수집 결과 | Supabase 테이블 내용 대조 |

## 8. 결정 사항 및 남은 항목

**확정** (2026-08-13, 사용자 승인)

| 항목 | 결정 |
|---|---|
| PRD 방향 | 승인 |
| Supabase 프로젝트 | `26 삼성증권 입문과정` (`qplxvfwuobvnobqpivbm`, 서울) |
| 감시 법령 | 16개 (3.1) — 기존 13 + 장애인고용촉진 · 고용상 연령차별금지 · 채용절차 공정화 |
| 계정 발급 | 관리자 발급. 화면에 가입 기능 없음 |
| 읽기 권한 | ~~로그인 필수~~ → **2026-08-13 변경: 로그인은 선택.** 브리핑은 공개, 담당자 노트·큐레이션만 로그인 (2항 표) |

**남은 항목**

| # | 항목 | 상태 |
|---|---|---|
| 1 | 법제처 `OC` 키 | ⚠️ **사용자 신청 필요.** 없으면 태스크 4(수집) 착수 불가 |
| 2 | `disable_signup` 끄기 | ⚠️ 사용자가 Supabase 대시보드에서 조치 (코드로 불가). 화면에는 가입 기능이 없지만 API 경로는 아직 열려 있다 |
| 2-1 | 계정 발급 | ✅ 완료 (태스크 3). `auth.users` 에 `email_confirmed_at` 을 채워 넣는 방식으로, 확인 메일 경로를 지나지 않는다 |
| 3 | 산업군 태그 키워드 | 3.4 초안 — 태스크 6 착수 전 검토 |
| 4 | AI 요약 사용 여부 | 태스크 7에서 판단. 비용·정확성 위험 재확인 |
| 5 | 한글 파일명 배포 | 헬스케어 태스크 13이 미검증. 문제 생기면 ASCII 파일명으로 전환 |
| 6 | `고용` 의 해석 | 「고용상 연령차별금지…」로 반영함. 「외국인근로자의 고용…」이었다면 교체 필요 |

## 9. 알려진 제약

- **법률 자문이 아니다.** 원문 전달과 요약까지이며, 판단은 담당자·전문가의 몫이다
- 법제처 개발계정 트래픽은 일 10,000건. 수집 주기·범위가 이를 넘지 않아야 한다
- API 응답 지연·장애 시 그날 수집이 비어 있을 수 있다 → 마지막 수집 시각을 화면에 노출해
  "오늘 갱신 안 됨"을 담당자가 알 수 있게 한다
- 수집은 API가 제공하는 범위만 가능하다. 대법원 미공개 판례·부처 내부 지침은 들어오지 않는다
- AI 요약은 오류 가능성이 있다 (3.6 원칙 참조)
- **저장소도 공개돼 있다** (`cjungwon7-wq/personal-dashboards`, 2026-08-13 public 전환).
  화면 코드와 publishable 키를 누구나 볼 수 있으므로, 방어는 전적으로 RLS 가 맡는다.
  코드에 비밀값을 두지 않는 규칙이 여기서 실제 조건이 된다
- 사이트는 공개 인터넷에 열려 있고, **브리핑은 누구나 볼 수 있다** (공개 자료라 그렇게 정했다).
  차단되는 것은 담당자 노트·큐레이션이며, 그 자격은 **로그인 + `labor_members` 등록**이다.
  사내망 제한(IP)은 하지 않는다 — Vercel 정적 호스팅으로는 어렵고,
  재택·외근 중 열람을 막아 실용성이 떨어진다
- 계정을 받은 사람이 화면을 캡처해 외부로 옮기는 것은 막을 수 없다.
  담당자 코멘트는 사내 정보라는 점을 전제로 작성해야 한다
- 비밀번호 재설정은 이메일 발송이 필요해 동작하지 않는다 (`mailer_autoconfirm: false`).
  잊으면 관리자가 대시보드에서 재설정한다
