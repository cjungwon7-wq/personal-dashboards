# 태스크 목록 — 관심종목 모닝 대시보드

관련 문서: [PRD.md](./PRD.md) · 산출물: `관심종목_대시보드.html`
최종 갱신: 2026-08-12

**완료 기준은 PRD 7항을 따른다** — 브라우저에서 해당 기능이 실제로 동작하면 완료.

---

## 진행 현황

| # | 태스크 | 상태 | 검증 |
|---|---|---|---|
| 1 | 화면 뼈대 | ✅ 완료 | 레이아웃 렌더링 확인 |
| 2 | 관심종목 등록 · 삭제 | ✅ 완료 | 4/4 통과 |
| 3 | 메모 기록 | ✅ 완료 | 3/3 통과 |
| 4 | 오늘의 체크리스트 | ✅ 완료 | 4/4 통과 |
| 5 | 디자인 (Samsung Blue · 카테고리 색 구분) | ✅ 완료 | 라이트/다크 렌더링 확인 |

**전체 자동 검증: 11 / 11 통과**

---

## 태스크 1 — 화면 뼈대 ✅

상단 배너(인사·날짜), 좌측 관심종목 카드, 우측 메모·체크리스트 카드의 2단 그리드.
860px 이하에서는 1단으로 전환.

**완료 기준**: PRD 4항 화면 구성대로 렌더링될 것 → 충족

---

## 태스크 2 — 관심종목 등록 · 삭제 ✅

**완료 기준 및 검증 결과**

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 종목 등록 | 3건 입력 → 목록 3개 | PASS |
| 중복 차단 | 동일 종목명+코드 재입력 → 3개 유지 | PASS |
| 종목 삭제 | 4번째 등록 후 `×` → 3개 복귀 | PASS |
| 저장 지속성 | localStorage에 3건 반영 | PASS |

---

## 태스크 3 — 메모 기록 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 메모창 표시 | 종목 클릭 → textarea 노출 | PASS |
| 메모 저장 | 2건 저장 → 목록 2건, 배지 갱신 | PASS |
| 메모 삭제 | `×` → 1건 감소 | PASS |

---

## 태스크 4 — 오늘의 체크리스트 ✅

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 기본 항목 로드 | 최초 실행 시 4개 생성 | PASS |
| 체크 토글·진행률 | 2개 체크 → `2 / 4` 표시 | PASS |
| 항목 추가 | 입력 → 5개 | PASS |
| 항목 삭제 | `×` → 4개 | PASS |

> 일자 변경 시 체크 해제 로직(`rolloverIfNewDay`)은 날짜 조작이 필요해 자동 검증에서 제외됨. 실사용 다음 날 확인 필요.

---

## 태스크 5 — 디자인 ✅

Samsung Blue(`#1428A0`) 계열 전면 적용, 상단 네이비 그라데이션 배너,
카드별 좌측 컬러 바·제목 점·배지로 카테고리 구분, 카드 그림자와 14px 라운드.

---

## 백로그 (다음 버전 후보)

| 우선순위 | 항목 | 메모 |
|---|---|---|
| 상 | 가격 알림 메모 | "6,300 이하면 재검토" 같은 조건 메모를 상단 고정 |
| 상 | 일자 변경 체크 해제 실검증 | 태스크 4의 미검증 항목 |
| 중 | 메모 검색 | 종목 수가 늘면 필요 |
| 중 | 체크리스트 요일별 프리셋 | 월요일/월말 등 루틴 분기 |
| 하 | 종목 순서 변경 (드래그) | |
| 보류 | 실시간 시세 연동 | PRD 5항 제외 범위. 서버나 프록시 필요 |

---

## 검증 실행 방법

```powershell
$src = "관심종목_대시보드.html"; $inj = "_test_inject.js"
$tmp = "$env:TEMP\dashtest"; New-Item -ItemType Directory -Force $tmp | Out-Null
$html = [IO.File]::ReadAllText($src, [Text.Encoding]::UTF8)
$js   = [IO.File]::ReadAllText($inj, [Text.Encoding]::UTF8)
[IO.File]::WriteAllText("$tmp\test.html",
  $html.Replace('</body>', "<script>$js</script></body>"),
  (New-Object Text.UTF8Encoding($false)))
# 생성된 test.html을 브라우저로 열면 하단에 통과/실패 결과가 표시됨
```

기능을 수정한 뒤에는 `_test_inject.js`에 검증 항목을 추가하고 위 절차를 다시 실행할 것.
